diff --git a/node_modules/proton-native/bin/backends/qt.js b/node_modules/proton-native/bin/backends/qt.js
index 3e42d75..107b772 100644
--- a/node_modules/proton-native/bin/backends/qt.js
+++ b/node_modules/proton-native/bin/backends/qt.js
@@ -198,6 +198,18 @@ var TextInputElement = /** @class */ (function (_super) {
     TextInputElement.prototype.setText = function (text) {
         this.element.setText(text);
     };
+    TextInputElement.prototype.scrollToBottom = function () {
+        if (this.element.scrollToBottom) {
+            this.element.scrollToBottom();
+        }
+    };
+    TextInputElement.prototype.setReadOnly = function (readOnly) {
+        if (readOnly) {
+            this.element.setFocusPolicy(0); // Qt::NoFocus
+        } else {
+            this.element.setFocusPolicy(3); // Qt::StrongFocus (default for input)
+        }
+    };
     return TextInputElement;
 }(BaseElement));
 exports.TextInputElement = TextInputElement;
diff --git a/node_modules/proton-native/bin/components/TextInput.js b/node_modules/proton-native/bin/components/TextInput.js
index 106caeb..c7b31ef 100644
--- a/node_modules/proton-native/bin/components/TextInput.js
+++ b/node_modules/proton-native/bin/components/TextInput.js
@@ -1,6 +1,6 @@
 "use strict";
 var __assign = (this && this.__assign) || function () {
-    __assign = Object.assign || function(t) {
+    __assign = Object.assign || function (t) {
         for (var s, i = 1, n = arguments.length; i < n; i++) {
             s = arguments[i];
             for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
@@ -22,13 +22,17 @@ exports.default = (function (p) {
         style: PropTypes.object,
         onChangeText: PropTypes.func,
         value: PropTypes.string,
-        multiline: PropTypes.bool
+        multiline: PropTypes.bool,
+        readOnly: PropTypes.bool,
+        autoScrollToBottom: PropTypes.bool
     };
     var defaultProps = {
         style: {},
         onChangeText: function () { },
         value: "",
-        multiline: false
+        multiline: false,
+        readOnly: false,
+        autoScrollToBottom: false
     };
     var TextInputElement = index_1.getBackend()["TextInputElement"];
     var element = new TextInputElement(p.multiline);
@@ -64,9 +68,17 @@ exports.default = (function (p) {
         },
         value: function (value) {
             element.setText(value);
+            if (p.autoScrollToBottom) {
+                element.scrollToBottom();
+            }
+        },
+        readOnly: function (readOnly) {
+            element.setReadOnly(readOnly);
         }
     });
     updateProps(props);
-    return __assign(__assign(__assign({}, containerProps), yogaProps), { element: element,
-        updateProps: updateProps });
+    return __assign(__assign(__assign({}, containerProps), yogaProps), {
+        element: element,
+        updateProps: updateProps
+    });
 });
diff --git a/node_modules/proton-native/bin/utils/convertStyleSheet.js b/node_modules/proton-native/bin/utils/convertStyleSheet.js
index 155cc81..a155a0c 100644
--- a/node_modules/proton-native/bin/utils/convertStyleSheet.js
+++ b/node_modules/proton-native/bin/utils/convertStyleSheet.js
@@ -38,7 +38,7 @@ var excluded = [
 ];
 var convertToPx = ["fontSize"];
 var convertStyleSheet = function (style) {
-    return Object.entries(style).reduce(function (styleString, _a) {
+    return Object.entries(style || {}).reduce(function (styleString, _a) {
         var propName = _a[0], propValue = _a[1];
         if (excluded.includes(propName)) {
             return styleString;
